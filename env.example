# AWS 프리티어 환경변수 설정 파일
# 이 파일을 복사해서 .env로 저장하고 실제 값으로 변경하세요

# RDS MySQL 설정 (AWS Console에서 확인)
export DB_HOST="<RDS-엔드포인트>.rds.amazonaws.com"
export DB_USERNAME="<마스터-사용자명>"
export DB_PASSWORD="<강력한-비밀번호>"

# Jasypt 암호화 키 (application-prod.yml 복호화용)
export JASYPT_PASSWORD="<기존-암호화-키>"

# 서버 포트 설정 (선택사항)
export APP_PORT=80

# Redis 설정 (선택사항)
export REDIS_PORT=6379
export REDIS_MEMORY=100mb

# OAuth2 설정 (기존 설정 그대로 사용)
# Google, GitHub, Naver 클라이언트 ID/Secret은 
# application-prod.yml에 이미 암호화되어 있음

# 이메일 설정 (기존 설정 그대로 사용)
# Gmail SMTP 설정도 application-prod.yml에 암호화되어 있음

# 사용법:
# 1. 이 파일을 .env로 복사: cp env.example .env
# 2. .env 파일의 값들을 실제 값으로 변경
# 3. 환경변수 로드: source .env
# 4. 배포 실행: ./deploy.sh
